# Alhawija 3.10.7

## تقوية الحماية

تم تفعيل R8 في نسخة Release مع تصغير الموارد وتشويش أسماء الكود، مع قواعد keep مخصصة لضمان بقاء واجهة WebView والخدمات والودجات ومستقبلات النظام والتحديثات تعمل بشكل صحيح.

تم تحويل إعدادات التطبيق إلى `EncryptedSharedPreferences` باستخدام Android Keystore مع ترحيل تلقائي لإعدادات الإصدارات القديمة. تشمل الحماية إعدادات الموقع، المواقيت المحفوظة، الصوت، الأذان، الكتم، إعدادات الودجات والتحديثات.

تمت إضافة فحص شهادة التوقيع الرسمية عند بدء التطبيق. النسخ Release المعاد توقيعها أو المعدلة لا تعمل، بينما تبقى نسخة Debug مسموحة للتطوير. كما أن مسار التحديث كان يتحقق مسبقًا من SHA-256 وتطابق توقيع APK الحالي قبل التثبيت، وتم الإبقاء على ذلك.

مفتاح التوقيع الخاص لم يتغير، لذلك يستطيع المطور إصدار تحديثات مستقبلية تقبلها النسخة الحالية. لا يمكن حماية APK العميل من التفكيك بنسبة 100%، لكن هذه الإجراءات تمنع التعديل السهل وإعادة التوقيع وتحمي البيانات المحلية وتقلل قابلية قراءة الكود.

## التحقق

تم تنفيذ `testDebugUnitTest` و`assembleRelease` بنجاح مع `minifyReleaseWithR8` و`shrinkResources`. تم التحقق من وجود ملف R8 mapping ومن توقيع APK عبر APK Signature Scheme v2.

| العنصر | القيمة |
|---|---|
| Version code | 468 |
| Version name | 3.10.7 |
| حجم APK | 9,107,640 بايت |
| حجم الإصدار السابق 3.10.6 | 13,750,292 بايت |
| SHA-256 | `28b69c5a9295df0845633300e72b56a9dde11fc0a2718d1018d501ebbe347cdb` |
| شهادة التوقيع SHA-256 | `2077b4e65ca5935e12a7f862f81cd8eee099c93d249f9578bcbcb73f9fafbfac` |
